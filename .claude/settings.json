{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,subprocess; d=json.load(sys.stdin); f=d.get('file_path',''); subprocess.run(['uv','run','ruff','check','--fix',f]) if f.endswith('.py') else None\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); f=d.get('file_path',''); blocked=any(p in f for p in ['progress/data','items_rules.default.json']); print('BLOCK: use uv run python scripts/update_snapshot_and_defaults.py instead') if blocked else None; sys.exit(1 if blocked else 0)\""
          }
        ]
      }
    ]
  }
}
