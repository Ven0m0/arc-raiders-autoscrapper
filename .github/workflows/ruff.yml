name: CI
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install mise
        run: |
          curl https://mise.run | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: Setup tools
        run: |
          mise install python@3.13
          mise install uv@latest
      - name: "Set up Python"
        run: mise exec python@3.13 uv@latest -- python --version
      - name: Install uv
        run: mise exec python@3.13 uv@latest -- uv --version
      - name: Install dependencies
        run: mise exec python@3.13 uv@latest -- uv sync --frozen --all-extras
      - name: Run Ruff
        run: mise exec python@3.13 uv@latest -- uv run ruff check src/ tests/ scripts/
      - name: Run basedpyright
        run: mise exec python@3.13 uv@latest -- uv run basedpyright src/
      - name: Run ty
        run: mise exec python@3.13 uv@latest -- uv run ty check src/
      - name: Run pytest
        run: mise exec python@3.13 uv@latest -- uv run pytest
